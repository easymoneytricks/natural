import crypto from 'node:crypto'
import { env } from '../config/env.js'
export function paymentMethods(req,res){ res.json({ data: { cod: true, online: env.razorpay.enabled, razorpayKeyId: env.razorpay.enabled ? env.razorpay.keyId : null } }) }
export async function createRazorpayOrder(req,res,next){ try { if(!env.razorpay.enabled) { const e=new Error('Online payment is not available yet.'); e.statusCode=503; e.code='RAZORPAY_UNAVAILABLE'; throw e } const e=new Error('Razorpay order initialization requires configured production credentials.'); e.statusCode=501; e.code='RAZORPAY_NOT_CONFIGURED'; throw e } catch(e){next(e)} }
export async function verifyRazorpay(req,res,next){ try { if(!env.razorpay.enabled) { const e=new Error('Online payment is not available yet.'); e.statusCode=503; e.code='RAZORPAY_UNAVAILABLE'; throw e } const { razorpay_order_id:orderId,razorpay_payment_id:paymentId,razorpay_signature:signature }=req.body||{}; if(!orderId||!paymentId||!signature) { const e=new Error('Invalid payment verification request.'); e.statusCode=400;e.code='PAYMENT_VERIFICATION_FAILED';throw e } const expected=crypto.createHmac('sha256',env.razorpay.keySecret).update(`${orderId}|${paymentId}`).digest('hex'); if(!crypto.timingSafeEqual(Buffer.from(expected),Buffer.from(signature))) { const e=new Error('Payment verification failed.');e.statusCode=400;e.code='PAYMENT_VERIFICATION_FAILED';throw e } res.json({data:{verified:true}}) }catch(e){next(e)} }
export function webhook(req,res,next){ try { if(!env.razorpay.enabled) return res.status(503).json({error:{code:'RAZORPAY_UNAVAILABLE',message:'Online payment is not available yet.'}}); const signature=req.get('x-razorpay-signature')||''; const raw=req.rawBody; const expected=crypto.createHmac('sha256',env.razorpay.webhookSecret).update(raw).digest('hex'); if(!signature||!crypto.timingSafeEqual(Buffer.from(expected),Buffer.from(signature))) return res.status(400).json({error:{code:'INVALID_WEBHOOK_SIGNATURE',message:'Invalid webhook signature.'}}); return res.json({data:{received:true}}) }catch(e){next(e)} }
