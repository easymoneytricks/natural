# VITE_* values are public and embedded into browser JavaScript at build time.
# Never place JWT secrets, database credentials, SMTP credentials, or payment secrets here.
VITE_API_BASE_URL=http://localhost:4000/api/v1
VITE_STOREFRONT_URL=http://localhost:5173
