# VITE_* values are embedded into browser JavaScript at build time.
# Never put passwords, JWT secrets, database credentials, SMTP credentials,
# payment secrets, or reCAPTCHA secret keys in this file.
VITE_API_BASE_URL=http://localhost:4000/api/v1
VITE_SITE_URL=http://localhost:5173
