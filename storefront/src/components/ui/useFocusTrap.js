import {useEffect} from 'react'
export function useFocusTrap(ref,active,onClose){useEffect(()=>{if(!active||!ref.current)return;const dialog=ref.current;const prior=document.activeElement;const focusables=()=>[...dialog.querySelectorAll('a[href],button:not([disabled]),input:not([disabled])')];const first=focusables()[0];first?.focus();const handleKey=(event)=>{if(event.key==='Escape')onClose();if(event.key!=='Tab')return;const items=focusables();if(!items.length)return;const start=items[0],end=items[items.length-1];if(event.shiftKey&&document.activeElement===start){event.preventDefault();end.focus()}else if(!event.shiftKey&&document.activeElement===end){event.preventDefault();start.focus()}};dialog.addEventListener('keydown',handleKey);return()=>{dialog.removeEventListener('keydown',handleKey);prior?.focus?.()}},[ref,active,onClose])}
