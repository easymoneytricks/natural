# Deployment

Target deployment is cPanel-style hosting: build the Vite storefront and Admin, run the Node backend, configure MariaDB, environment variables, HTTPS, CORS origins, writable managed media storage, migrations, and the Razorpay webhook endpoint. Use provider secrets; no client domain is assumed here.
